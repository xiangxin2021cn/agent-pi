# Agent Pi V2.2 Enterprise Tender Workbench Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver the first V2.2 Tender Intelligence Core vertical slice with a versioned project-local business model, deterministic readiness audit, application-owned persistence tool, and bounded professional skill.

**Architecture:** Add a small dependency-neutral `@agent-pi/business-core` package for pure tender schemas and audits. Adapt it into the existing session-tool registry through one project-local `tender_workspace` tool. Keep model state in inspectable JSON under the selected working directory and leave Goal Loop, document delivery, knowledge sources, and provider backends unchanged.

**Tech Stack:** TypeScript, Zod 4, Bun test, existing session-tools registry, project-local JSON artifacts, existing Skill format.

## Global Constraints

- Working directories are output/project boundaries, not implicit source corpora.
- Only selected sources, attached files, and explicitly registered files may enter the Tender Workspace.
- No nested sub-agent spawning.
- No hard-coded rates or productivity benchmarks in Skill instructions.
- No licensed standard text bundled into the repository.
- Every requirement and evaluation criterion must preserve an exact source locator.
- Invalid cross-entity references must be rejected before persistence.
- Writes must be atomic and contained under `<working-directory>/.agent-pi/business/tender/`.
- The first slice must remain useful with both Claude and Pi/Claude-compatible models.

---

### Task 1: Create The Pure Tender Business Model

**Files:**
- Create: `packages/business-core/package.json`
- Create: `packages/business-core/tsconfig.json`
- Create: `packages/business-core/src/index.ts`
- Create: `packages/business-core/src/tender/types.ts`
- Create: `packages/business-core/src/tender/schema.ts`
- Create: `packages/business-core/src/tender/index.ts`
- Test: `packages/business-core/src/tender/schema.test.ts`

**Interfaces:**
- Produces: `TenderWorkspace`, `TenderSourceLocator`, `TenderDocument`, `TenderRequirement`, `TenderEvaluationCriterion`, `TenderDeliverable`, `TenderResponsePlan`, `TenderWorkspaceSchema`, `parseTenderWorkspace(value)`.

- [ ] Write tests that accept a minimal version-1 workspace and reject duplicate IDs, malformed IDs, invalid dates, invalid weights, and source locators without a document ID.
- [ ] Run `bun test packages/business-core/src/tender/schema.test.ts` and verify failure because the package and parser do not exist.
- [ ] Implement the minimum types and Zod schemas. Use discriminated enums for requirement type, criterion method, and entity status.
- [ ] Run the test and `cd packages/business-core && bun run tsc --noEmit`.
- [ ] Commit as `feat: add tender workspace business model`.

### Task 2: Add Deterministic Tender Readiness Audit

**Files:**
- Create: `packages/business-core/src/tender/audit.ts`
- Test: `packages/business-core/src/tender/audit.test.ts`
- Modify: `packages/business-core/src/tender/index.ts`

**Interfaces:**
- Consumes: `TenderWorkspace` from Task 1.
- Produces: `auditTenderWorkspace(workspace): TenderReadinessAudit` and readiness states `not_ready | needs_review | ready`.

- [ ] Write tests for broken references, superseded-source citations, uncovered mandatory requirements, uncovered weighted criteria, unlinked deliverables, verified responses without evidence, and a complete ready workspace.
- [ ] Run the audit test and verify it fails because `auditTenderWorkspace` does not exist.
- [ ] Implement deterministic entity indexing, issue codes, count summaries, criterion-weight coverage, and readiness classification.
- [ ] Run schema and audit tests together.
- [ ] Commit as `feat: audit tender workspace readiness`.

### Task 3: Implement Project-Local Tender Workspace Persistence Tool

**Files:**
- Create: `packages/session-tools-core/src/handlers/tender-workspace.ts`
- Test: `packages/session-tools-core/src/handlers/tender-workspace.test.ts`
- Modify: `packages/session-tools-core/package.json`
- Modify: `packages/session-tools-core/src/handlers/index.ts`
- Modify: `packages/session-tools-core/src/tool-defs.ts`
- Modify: `packages/session-tools-core/src/index.ts`

**Interfaces:**
- Consumes: `parseTenderWorkspace`, entity schemas, and `auditTenderWorkspace` from `@agent-pi/business-core`.
- Produces: registry tool `tender_workspace` with actions `init`, `upsert_documents`, `upsert_requirements`, `upsert_criteria`, `upsert_deliverables`, `upsert_responses`, `status`, and `validate`.

- [ ] Write handler tests using a temporary working directory. Assert the exact storage path, atomic replacement, revision increment, restart readability, upsert behavior, invalid-reference rejection, and path traversal rejection.
- [ ] Run the handler test and verify failure because the tool does not exist.
- [ ] Implement safe project ID handling, project-local paths, atomic JSON writes, full-model validation before writes, and audit rewrite after each mutation.
- [ ] Register the schema and handler in the canonical tool registry with execution-mode writes.
- [ ] Run handler tests and `cd packages/session-tools-core && bun run tsc --noEmit`.
- [ ] Commit as `feat: add project tender workspace tool`.

### Task 4: Add Tender Intelligence Core Skill

**Files:**
- Create: `.agents/skills/tender-intelligence-core/SKILL.md`
- Create: `.agents/skills/tender-intelligence-core/references/workflow.md`
- Create: `.agents/skills/tender-intelligence-core/references/data-model.md`
- Create: `.agents/skills/tender-intelligence-core/references/readiness-gates.md`
- Test: `packages/shared/src/skills/__tests__/tender-intelligence-core.test.ts`

**Interfaces:**
- Consumes: the `tender_workspace` tool and selected source boundary.
- Produces: a bounded source-first tender intake and compliance workflow.

- [ ] Write a test that loads the project Skill and checks the exact guardrails: selected sources only, register before analyze, exact locators, no hard-coded rates, no working-directory scan, human pause on ambiguity, and deterministic validation before completion.
- [ ] Run the test and verify failure because the Skill is absent.
- [ ] Write the focused Skill and references; keep operational detail in references rather than a monolithic SKILL body.
- [ ] Run the Skill test and existing skill storage/validation tests.
- [ ] Commit as `feat: add tender intelligence core skill`.

### Task 5: Connect Tender Readiness To Goal Audit Evidence

**Files:**
- Create: `packages/server-core/src/sessions/tender-workspace-evidence.ts`
- Test: `packages/server-core/src/sessions/tender-workspace-evidence.test.ts`
- Modify: `packages/server-core/src/sessions/goal-controller.ts`
- Test: `packages/server-core/src/sessions/goal-controller.test.ts`

**Interfaces:**
- Consumes: tool results containing `modelPath`, `auditPath`, and `readiness`.
- Produces: bounded `tender_workspace_readiness` audit evidence and deterministic failure when a user explicitly requests a submission-ready tender package but readiness is not `ready`.

- [ ] Write focused parser tests for successful, malformed, and stale tool-result payloads.
- [ ] Write a Goal Controller test proving that ordinary tender analysis may finish with `needs_review`, while an explicit submission-ready request cannot pass unless Tender Workspace is ready.
- [ ] Run tests and verify expected failures.
- [ ] Implement a small evidence adapter; do not place tender parsing logic in Goal Controller.
- [ ] Run focused Goal Controller tests.
- [ ] Commit as `feat: gate submission-ready tender delivery`.

### Task 6: Add Domain Regression Fixtures

**Files:**
- Create: `packages/business-core/src/tender/fixtures/complete-tender.json`
- Create: `packages/business-core/src/tender/fixtures/incomplete-tender.json`
- Create: `packages/business-core/src/tender/regression.test.ts`
- Create: `docs/V2.2_ENTERPRISE_TENDER_PLUGIN_ROADMAP.md`

**Interfaces:**
- Consumes: all prior model and audit interfaces.
- Produces: stable fixtures for future BOQ, programme, resource, cost, and cash-flow packs.

- [ ] Add one complete and one deliberately incomplete construction tender fixture using synthetic content only.
- [ ] Assert deterministic audit results and issue codes against both fixtures.
- [ ] Document the seven capability packs, dependency order, shared IDs, output artifacts, and acceptance gates.
- [ ] Run all `business-core` tests.
- [ ] Commit as `test: add tender intelligence regression pack`.

### Task 7: Verify The V2.2 Vertical Slice

**Files:**
- Modify only files required to resolve failures caused by Tasks 1-6.

**Interfaces:**
- Consumes: the completed V2.2 slice.
- Produces: verified branch state ready for user testing, not yet a public release.

- [ ] Run `bun test packages/business-core packages/session-tools-core/src/handlers/tender-workspace.test.ts packages/shared/src/skills/__tests__/tender-intelligence-core.test.ts packages/server-core/src/sessions/tender-workspace-evidence.test.ts`.
- [ ] Run `bun run typecheck:all`.
- [ ] Run the existing orchestration, evidence-matrix, document-artifact, template, export, and schedule regression tests.
- [ ] Review `git diff --check`, `git status --short`, and the full branch diff for unrelated changes.
- [ ] Commit verification or fixes as `chore: verify v2.2 tender intelligence core` only when a tracked change is required.

## Post-MVP Plans

The following require separate implementation plans after Tender Intelligence Core is proven:

1. BOQ-Specification-Drawing Reconciliation
2. Integrated CPM and Resource Engine
3. Cost Build-up and Price Library
4. Contract-aware Cash-Flow Engine
5. Construction Methodology Composer
6. Submission Assembly and Red-Team UI
7. Post-award Project Controls and Forecasting

