# v1.1.1 - Native Dialog Timeout Fix

## Fixes

- Removed the client-side RPC timeout for user-driven native file and folder pickers.
- File attachment, folder attachment, and working-directory selection dialogs now wait until the user chooses or cancels instead of failing after the default request timeout.
- Kept the default timeout for normal RPC calls so real stalled requests still fail visibly.

