# v0.10.8 - Claude Agent Runtime Stability

## Improvements

- Upgraded `@anthropic-ai/claude-agent-sdk` from `0.3.170` to `0.3.191`, bringing the bundled Claude Code runtime from `2.1.170` to `2.1.191`.
- Upgraded `@anthropic-ai/sdk` from `0.100.0` to `0.106.0`.
- Pulls in upstream stability fixes for background agents/workflows, SDK-hosted MCP handling, tool permission metadata, fast mode persistence, and richer rate-limit/usage metadata.

## Validation

- Verified SDK event adaptation, tool matching, background task handling, SDK error mapping, runtime binary lookup, TypeScript checks, and Electron production build.

## Breaking Changes

- None.
