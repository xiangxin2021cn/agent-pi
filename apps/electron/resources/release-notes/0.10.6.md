# v0.10.6 - Session Artifact Preview Hotfix

## Fixes

- Fixed in-app file preview for conversation artifacts stored under a session working directory, including `Agent Pi Outputs`.
- Kept the file preview security boundary intact: arbitrary external files remain blocked, while registered session directories, session working directories, and formal output directories are allowed.
- Applied the same session-aware path allowlist to text/Markdown previews, image data URLs, lightweight preview thumbnails, PDF binary reads, and attachment reads.

## Tests

- Added coverage for formal output preview path allowlisting and unrelated external path denial.
